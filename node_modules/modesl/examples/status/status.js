#!/usr/bin/env node

var esl = require('modesl'),
//open a connection
conn = new esl.Connection('127.0.0.1', 8021, 'ClueCon', function() {
    //send the status api command
    //conn.api('status', function(res) {
        //log result body and exit
       // console.log(res.getBody());
        //process.exit(0);
    //});
    conn.api('callcenter_config queue list', function(res) {
        //log result body and exit
        console.log(res.getBody());
        //process.exit(0);
    });
    conn.api('callcenter_config queue count agents queue-12345 status', function(res) {
        //log result body and exit
        console.log(res.getBody());
    });
    conn.api('callcenter_config queue list agents queue-12345', function(res) {
        //log result body and exit
        console.log(res.getBody());
        process.exit(0);
    });
});
